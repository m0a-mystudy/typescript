describe('TypeScript仕様チェック', () => {
    it('宣言方法確認1', (done) => {
        let tables = [
            'a',
            'b',
            'c'
        ];
        let select_columns = {
            'a': ['a1', 'a2'],
            'b': ['b1', 'b2'],
            'c': ['c1', 'c2']
        };
        for (let table of tables) {
        }
        done();
    });
});
//# sourceMappingURL=typescript_basic_test.js.map